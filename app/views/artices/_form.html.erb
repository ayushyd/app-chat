<%= form_with(model: artice) do |form| %>
  <% if artice.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artice.errors.count, "error") %> prohibited this artice from being saved:</h2>

      <ul>
        <% artice.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
